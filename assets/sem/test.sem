#include "irgen/sem.h"

DECL_NODE(
    ExprNode, Expr,
    {
        info << 2;
    },
    2);
DECL_NODE(
    Expr1Node, Expr1, { info << 3; }, 2);
DECL_NODE(
    Expr2Node, Expr2, { info << 4; }, 2);

REG_NODES(3, ExprNode, Expr1Node, Expr2Node);