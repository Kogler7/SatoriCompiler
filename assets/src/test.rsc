int main()
{

    int x = 10;                // integer variable
    int y = +3e10;             // integer variable in scientific notation
    int z = 0x3f;              // integer variable in hexadecimal notation
    real o = 3.14;             // float variable
    real p = -2.71828;         // double variable
    real q = 1.0e-10;          // double variable in scientific notation
    str s = "Hello World!"; // string variable

    int result = x + 2 * (y - z);

    if (x > y)
    {
        1 + 1;
    }
    else
    {
    }

    /*
    block comment 1
    */

    for (int i = 0; i < x; i++)
    {
        print i;
    }

    // line comment

    return 0;
}